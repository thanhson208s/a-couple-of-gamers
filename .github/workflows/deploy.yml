name: Deploy

on:
  push:
    branches:
      - dev
    tags:
      - "v*"

jobs:
  deploy-vps:
    name: Deploy NestJS to VPS
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Determine environment
        id: env
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "env=production" >> $GITHUB_OUTPUT
          else
            echo "env=staging" >> $GITHUB_OUTPUT
          fi

      - name: Build and push Docker image
        run: |
          echo "${{ secrets.REGISTRY_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker build -f server/Dockerfile -t ghcr.io/${{ github.repository }}/server:${{ github.sha }} .
          docker push ghcr.io/${{ github.repository }}/server:${{ github.sha }}

      - name: Deploy
        run: |
          cd /opt/acog
          docker pull ghcr.io/${{ github.repository }}/server:${{ github.sha }}
          docker compose -f docker-compose.yml -f docker-compose.${{ steps.env.outputs.env }}-app.yml up -d
          docker compose exec app node dist/app --run-migrations || true
          curl -f http://localhost:3000/health

  publish-assets:
    name: Publish Cocos assets to R2
    runs-on: ubuntu-latest
    concurrency:
      group: "bundle-publish-${{ startsWith(github.ref, 'refs/tags/') && 'production' || 'staging' }}"
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4

      - name: Determine environment
        id: env
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "env=production" >> $GITHUB_OUTPUT
          else
            echo "env=staging" >> $GITHUB_OUTPUT
          fi

      # Cocos Creator CLI is not yet available in CI.
      # These steps are placeholders — replace with actual Cocos headless build
      # commands once the build pipeline is configured.
      - name: Build Cocos main bundle (placeholder)
        run: | 
          echo "TODO: cocos-creator --build --platform web-mobile --configPath client/build-config.json"

      - name: Generate hot-update manifest (placeholder)
        run: |
          echo "TODO: run hot-update manifest tool against previous project.manifest"

      - name: Upload hot-update assets to R2 (placeholder)
        run: echo "TODO: rclone copy hot-update/assets r2:${{ secrets.R2_BUCKET_NAME }}/hot-update/${{ steps.env.outputs.env }}/assets/"

      - name: Purge manifest CDN cache
        run: |
          curl -X DELETE "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data "{\"files\":[\"https://acob.gootube.online/hot-update/${{ steps.env.outputs.env }}/version.manifest\",\"https://acob.gootube.online/hot-update/${{ steps.env.outputs.env }}/project.manifest\"]}"

      # Whole-bundle publish — see docs/workflow.md#publishing-a-game-bundle.
      # Every push rebuilds every slug, content-hashes each source dir, uploads
      # (skip-if-exists), PUTs the manifest (atomic), then prunes orphans.
      - name: Build game bundles (placeholder)
        run: |
          echo "TODO: for each client/res/games/<slug>/, run Cocos bundle build and compute <hash> from source dir"

      - name: Upload bundles to R2 — skip-if-exists (placeholder)
        run: |
          echo "TODO (per slug): rclone copy <bundle> r2:${{ secrets.R2_BUCKET_NAME }}/game-bundles/${{ steps.env.outputs.env }}/<slug>/<hash>/ --ignore-existing"

      - name: Compose manifest.json (placeholder)
        run: |
          echo "TODO: compose in-memory { generatedAt, games: { <slug>: { version: <hash>, url: <versioned-url> } } } from build output"

      - name: PUT manifest.json to R2 (placeholder)
        run: |
          echo "TODO: rclone copy manifest.json r2:${{ secrets.R2_BUCKET_NAME }}/game-bundles/${{ steps.env.outputs.env }}/manifest.json"

      - name: Prune unreferenced bundle versions (placeholder)
        run: |
          echo "TODO: for each <slug>, list game-bundles/${{ steps.env.outputs.env }}/<slug>/*/ and delete every <version>/ not referenced by the new manifest"

      - name: Purge manifest.json CDN cache
        run: |
          curl -X DELETE "https://api.cloudflare.com/client/v4/zones/${{ secrets.CLOUDFLARE_ZONE_ID }}/purge_cache" \
            -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data "{\"files\":[\"https://acob.gootube.online/game-bundles/${{ steps.env.outputs.env }}/manifest.json\"]}"
